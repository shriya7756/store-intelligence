version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./store.db:/app/store.db
    environment:
      - PYTHONUNBUFFERED=1
    command: bash -c "python app/seed.py && uvicorn app.main:app --host 0.0.0.0 --port 8000"
